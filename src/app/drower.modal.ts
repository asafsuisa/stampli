import { StringifyOptions } from 'querystring';

export interface DrowerElement {
  col?: number;
  row?: number;
  label?: string;
  domType?: string;
  selectOptions?: string[];
  placeholder?: string;
}

